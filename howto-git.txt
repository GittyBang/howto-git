git init

# add to staging area:
git add { . | filelist }

# add to commit area:
git commit -m "describing message for this commit"

# add to remote site (publish):
git remote add origin https://github.com/GittyBang
git push -u origin master

git pull origin master


git remote -v

git status
git log

